{
  "name": "lmath",
  "version": "1.4.0",
  "description": "Graph functions, systems, derivatives and integrals in interactive blocks, explore the unit circle, and typeset vector notation.",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --outfile=main.js --external:obsidian --format=cjs --platform=node --target=es2020 --loader:.ttf=dataurl",
    "test": "esbuild tests/motor.test.ts --bundle --platform=node --format=cjs --outfile=tests/.bundle.cjs && node tests/.bundle.cjs",
    "test:zoom": "esbuild tests/zoom.test.ts --bundle --platform=node --format=cjs --outfile=tests/.bundle.zoom.cjs && node tests/.bundle.zoom.cjs",
    "test:todo": "npm run test && npm run test:zoom",
    "trazar": "esbuild tools/trazar.ts --bundle --platform=node --format=cjs --outfile=tools/.trazar.cjs",
    "huella": "esbuild tools/huellaTrazado.ts --bundle --platform=node --format=cjs --outfile=tools/.huella.cjs",
    "medir": "esbuild tools/medirTrazado.ts --bundle --platform=node --format=cjs --outfile=tools/.medir.cjs",
    "sonda": "esbuild tools/sondaCas.ts --bundle --platform=node --format=cjs --outfile=tools/.sonda.cjs",
    "notacion": "esbuild tools/sondaNotacion.ts --bundle --platform=node --format=cjs --outfile=tools/.notacion.cjs",
    "bateria": "esbuild tests/bateria-cas.ts --bundle --platform=node --format=cjs --outfile=tests/.bundle.bateria.cjs && node tests/.bundle.bateria.cjs",
    "fuzz": "esbuild tests/fuzz-despeje.ts --bundle --platform=node --format=cjs --outfile=tests/.bundle.fuzz.cjs && node tests/.bundle.fuzz.cjs"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "math"
  ],
  "author": "Lenin",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.0",
    "esbuild": "^0.20.0",
    "obsidian": "1.13.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "mathjs": "^12.0.0"
  }
}
